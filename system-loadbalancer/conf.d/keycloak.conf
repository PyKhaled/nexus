# Reverse proxy for the Keycloak service (system-auth-keycloak).
# Commented out by default — enable only if Keycloak, not Authentik, is the
# active identity provider for this deployment (see CLAUDE.md "Auth Stack
# Note"). Running both proxied at once usually means one is a mistake.

# upstream keycloak {
#     server <hostname of your keycloak server>:8081;
#     keepalive 10;
# }

# server {
#     listen 80;
#     listen [::]:80;
#     server_name sso.domain.tld;
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name sso.domain.tld;

#     ssl_certificate /etc/letsencrypt/live/domain.tld/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/domain.tld/privkey.pem;
#     add_header Strict-Transport-Security "max-age=63072000" always;

#     location / {
#         proxy_pass http://keycloak;
#         proxy_http_version 1.1;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header Host $host;
#     }
# }
