# Copy to .env and set a strong, unique password before starting the stack.
# These are consumed directly by the official postgres image (env_file) —
# do not rename these keys.
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=change-me-keycloak-db-password
