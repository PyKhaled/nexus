# Values consumed by the active root compose.yml after `make collect-secrets`.
# Copy this file to .env, replace both change-me values, and never commit .env.

MYSQL_ROOT_PASSWORD=change-me-wordpress-root-password
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=change-me-wordpress-password
