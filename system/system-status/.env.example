# Generate a private value with: openssl rand -base64 32
KENER_SECRET_KEY=change-me-kener-development-secret-at-least-32-chars

# Must match the public URL used in the browser because Kener validates origins.
STATUS_DOMAIN=status.localhost
