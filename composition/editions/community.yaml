apiVersion: nexus.io/edition/v1alpha1
kind: Edition
id: community
description: Core self-hostable product foundation
defaults:
  gateway: nginx
  website: wordpress
  authentication: keycloak
required:
  - gateway
  - authentication
allowed:
  - gateway
  - website
  - authentication
  - service-status
entitlements: []
